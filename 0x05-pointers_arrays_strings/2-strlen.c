#include "main.h"
/**
 * _strlen -returns the length of a string.
 * @s: is the value of the string variable.
 *
 * Return: void.
 */
int _strlen(char *s)

{
	char string;
	int length;

	length = strlen(string);

	return (length);
}

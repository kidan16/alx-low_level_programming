#include "main.h"
#include <string.h>
/**
 * _strlen -returns the length of a string.
 * @s: is the value of the string variable.
 *
 * Return: void.
 */
int _strlen(char *s)

{
	int length;

	length = _strlen(char *s);

	return (length);
}
